# Python build / env
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
*.egg-info/
dist/
build/
.coverage

# Source exports and generated hub output (user data — never publish)
# NOTE: keep these patterns ANCHORED (leading /) — an un-anchored `context-hub/`
# would also match the bundled skill dir context_hub/skills/context-hub/.
/flomo.zip
/flomo*.zip
/*.zip
/out/
/context-hub/

# Agent / tooling local state (not part of context-hub)
.claude/
.codex/
.codex
.humanize/
.claude-research/
